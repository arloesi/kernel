package kernel.model

import kernel.network.Socket
import kernel.service.Remote

class Model {
    @Remote def subscribe(socket:Socket) {
    }

    @Remote def unsubscribe(socket:Socket) {
    }

    @Remote def create(socket:Socket) {
    }

    @Remote def update(socket:Socket) {
    }

    @Remote def destroy(socket:Socket) {
    }
}
